$('.stk').html("<strong>SQ:</strong>&nbsp<%= @lineitem.lineitem_sizes.stock_quantity %>"

# $('.stk').empty(); // Clear previous content
# <% @lineitem.lineitem_sizes.each do |lineitem_size| %>
#   $('.stk').append("<strong>SQ:</strong>&nbsp<%= lineitem_size.size.stock_quantity %>");
# <% end %>